export interface Character {
    _id: string;
    name: string;
    image: string;
    author: string; // Assuming you only need the author's ID
    artist: string; // Assuming you only need the artist's ID
  }
  